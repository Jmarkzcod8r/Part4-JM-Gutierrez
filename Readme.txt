type in terminal:
1. git clone 'https://github.com/Jmarkzcod8r/Part4-Chat-Assessment'
2. go to chat-back-end-server folder and open terminal and type `node index.js` to start server
3. Run `npm run start` inside src folder on client side.